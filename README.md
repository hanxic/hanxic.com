# My Website
This repo contains my personal website design.

Assuming one has the dependency, one needs to only run `hugo server -D` for running local dev server including drafts. 

`hugo` can be used to build the static site
